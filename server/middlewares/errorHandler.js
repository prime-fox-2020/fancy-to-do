
module.exports = function (err, req, res, next){
    let statusCode = 500;
    let errorCode =  'Unknown - Internal server error'

    if (condition) { //400
        
    } else if (abc){ //404
        
    }
}